# The local Postgres alternative this file used to provide is retired.
#
# The backend now talks to Azure SQL exclusively (AZURE_SQL_CONNECTIONSTRING
# in backend/.env) — there is no local Postgres path anymore, so there is
# nothing here to run. See sql/retail/002_create_orm_schema.sql for the
# retail/chat schema migration and src/db/db.py for the connection.
